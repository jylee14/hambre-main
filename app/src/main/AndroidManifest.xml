<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jun.hambre_main">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>

        <activity android:name=".Landing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
             <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login" android:label="@string/app_name"/>
        <activity android:name=".DietRestriction" android:label="@string/app_name"/>
        <activity android:name=".Preferences" android:label="@string/app_name"/>
        <activity android:name=".yelpStub" android:label="@string/app_name"/>
        <activity android:name=".FoodFinderController" android:label="Hambre"/>
        <activity android:name=".SelectRestaurantController" android:label="Hambre"/>
    </application>
</manifest>
